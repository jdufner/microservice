version: "2"
services:
  service-registry:
    build: service-registry/
    ports:
      - "8761:8761"
  gateway:
    build: gateway/
    ports:
      - "8080:8080"
    links:
      - service-registry
  monitor:
    build: monitor/
    ports:
      - "8180:8080"
    links:
      - service-registry
  hello-world:
    build: hello-world/
    links:
      - service-registry
  primes:
    build: primes/
    links:
      - service-registry

