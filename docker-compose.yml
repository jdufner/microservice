version: "2"
services:
  service-registry:
    build: service-registry/
    ports:
      - "8761:8761"
  gateway:
    build: gateway/
    ports:
      - "8080:8080"
    links:
      - service-registry
  monitor:
    build: monitor/
    ports:
      - "8180:8080"
    links:
      - service-registry
  git-ssh-server:
    image: unixtastic/git-ssh-server
    ports:
      - "2222:22"
    volumes:
      - "/tmp/git:/git"
  configuration:
    build: configuration/
    ports:
      - "8280:8080"
    links:
      - service-registry
      - git-ssh-server
  hello-world:
    build: hello-world/
    links:
      - service-registry
      - configuration
  primes:
    build: primes/
    links:
      - service-registry
      - configuration
